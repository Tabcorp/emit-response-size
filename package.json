{
  "name": "emit-response-size",
  "version": "1.0.0",
  "description": "Emit the response size as an event",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "TabDigital/emit-response-size",
  "keywords": [
    "event",
    "res",
    "server",
    "http",
    "response-size",
    "patch",
    "monkey"
  ],
  "license": "MIT",
  "dependencies": {
    "is-server-response": "^1.0.0"
  },
  "devDependencies": {
    "dependency-check": "^2.5.1",
    "get-server-port": "^1.0.0",
    "istanbul": "^0.4.0",
    "standard": "^5.3.1",
    "tape": "^4.2.2"
  },
  "files": [
    "index.js",
    "bin/*"
  ]
}
